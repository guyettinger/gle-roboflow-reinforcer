import { borderRadius, boxShadow, color, spacing, typography } from "./Variables";
import { CoinCounterTheme } from "./Theme.types";

export const LightTheme: CoinCounterTheme = {
    borderRadius,
    boxShadow,
    color,
    fonts: {
        family: 'NunitoSans, sans-serif',
    },
    name: 'light',
    spacing,
    typography
}